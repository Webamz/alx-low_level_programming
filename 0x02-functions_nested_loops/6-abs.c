#include "main.h"
/**
 * _abs - check main
 * @n: integer input
 * Description: This function returns absolute value of a number
 * Return: Absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
