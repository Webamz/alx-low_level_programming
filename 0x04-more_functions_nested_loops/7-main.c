#include "main.h"
/**
 * main - check the code for main school students
 * Return: Always 0
 */
int main(void)
{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	pint_diagonal(-4);
	return (0);
}
